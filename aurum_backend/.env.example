# Django Environment Configuration
DJANGO_ENVIRONMENT=development
DEBUG=True
SECRET_KEY=your-secret-key-here

# Database Configuration
USE_POSTGRESQL=False
DB_NAME=aurum_finance_dev
DB_USER=postgres
DB_PASSWORD=your-db-password
DB_HOST=localhost
DB_PORT=5432

# Production Settings (only needed for production)
ALLOWED_HOSTS=yourdomain.com,www.yourdomain.com
CORS_ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# Email Configuration (production)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-email-password
DEFAULT_FROM_EMAIL=noreply@aurumfinance.com

# Redis Configuration (production)
REDIS_URL=redis://127.0.0.1:6379/1

# SSL Configuration (production)
SECURE_SSL_REDIRECT=True

# API Keys Configuration
# OpenFIGI API key for Valley and IDB bank transformers
OPENFIGI_API_KEY=your-openfigi-api-key-here